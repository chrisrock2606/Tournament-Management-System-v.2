<UserControl x:Class="PresentationLayer.Views.PlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height='1*' />
            <RowDefinition Height='0.5*' />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" ItemsSource="{Binding XX}" SelectedIndex="{Binding XX}" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <!--<DataGridTextColumn Header="ID" Width="35" Binding="{Binding TournamentId}"/>
                <DataGridTextColumn Header="Turnerings Navn" MinWidth="100" Binding="{Binding TournamentName}"/>
                <DataGridTextColumn Header="Spil Navn" MinWidth="75" Binding="{Binding GameName}"/>-->
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</UserControl>
